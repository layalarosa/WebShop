@model WebShop.ViewModels.GamesListViewModel

<h1 class="mb-4">@ViewBag.CurrentCategory</h1>

<div class="row">
    @foreach (var game in Model.Games)
    {
        @await Html.PartialAsync("_GameCard", game)
    }
</div>

